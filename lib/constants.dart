import 'package:flutter/material.dart';

/// For showing/controlling global Snackbars.
final kGlobalScaffoldMessengerKey = GlobalKey<ScaffoldMessengerState>();

/// Constant duration for different animations.
const kAnimationDuration = Duration(milliseconds: 400);

/// Background image of the app.
const kDefaultBkgImage = 'assets/images/bkg-01.jpg';

/// Default duration in seconds per game round.
const kSecondsPerRound = 30;
